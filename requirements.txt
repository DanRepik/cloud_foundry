# Development dependencies live in pyproject.toml's
# [project.optional-dependencies].dev so that a single
#   pip install -e .[dev]
# is enough to run the full test suite, linters, and build tooling.
